version: "3"
services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      REDIS_URL: "db:6379"
    depends_on:
      - db

  db:
    image: redis:alpine
    ports:
      - "6379:6379"
