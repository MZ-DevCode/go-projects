services:
  caddy:
    image: caddy:alpine
    ports:
      - "80:80"
      - "443:443"  # Порт для HTTPS
    volumes:
      - ./index.html:/usr/share/caddy/index.html
      - ./Caddyfile:/etc/caddy/Caddyfile
      - ./secret:/usr/share/caddy/secret/
